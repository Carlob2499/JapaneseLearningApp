{
  "name": "@hikkoshi/schemas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Zod schemas and inferred types for Hikkoshi content packs — the single source of truth shared by the build-time pipeline and the app runtime.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "dependencies": {
    "zod": "^4.4.3"
  }
}
